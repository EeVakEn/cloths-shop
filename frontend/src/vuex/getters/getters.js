export default {
    PRODUCTS(state) {
        return state.products;
    },
    CATEGORIES(state) {
        return state.categories;
    },
    CART(state) {
        return state.cart;
    },
    FAVORITES(state) {
        return state.favorites;
    },
    BREADCRUMB(state) {
        return state.breadcrumb;
    }
}